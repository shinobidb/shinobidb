# shinobidb

本番DBのデータをマスキングしてステージング環境に自動同期するOSS CLIツール。

## 技術スタック

- TypeScript / Node.js
- CLIフレームワーク（未定）
- 初期対応DB: MySQL → 将来: PostgreSQL, MongoDB

## CLIコマンド構成

```
shinobidb scan    # DBに接続→スキーマ取得→PIIカラムを推論
shinobidb mask    # YAML定義に基づいてデータを匿名化
shinobidb config  # scanの結果からYAMLテンプレートを自動生成
```

## アーキテクチャ方針

- DB種類の抽象化層を設け、MySQL以外への拡張を容易にする
- マスキングルール生成は将来AI推論に置き換え可能なインターフェース
- 将来のSaaS化を見据えた設計（スケジュール実行、スキーマ変更検知）

## セッション管理

大きなタスクの自然な区切り（設計完了→実装開始、モジュールA完了→モジュールB開始など）で
セッションを切ることを提案する。提案時に以下を含む引き継ぎテキストを生成する:

- 完了済みの作業サマリー
- 現在のプロジェクト状態（何が動く状態か）
- 次セッションでやるべきこと（具体的なタスク）
- 注意点・未解決事項
- 関連ファイルパス
